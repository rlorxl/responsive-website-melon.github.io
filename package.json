{
  "scripts": {
    "start": "run-p watch server",
    "sass": "sass scss:css",
    "watch": "npm run sass -- --watch",
    "server": "live-server",
    "clean": "rimraf -r style",
    "build": "postcss css/style.css -o build/style.css"
  },
  "dependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.14",
    "postcss-cli": "^9.1.0",
    "postcss-combine-media-query": "^1.0.1",
    "rimraf": "^3.0.2",
    "sass": "^1.52.1",
    "sass-math": "^1.0.0"
  }
}
